# tool-site デザイン仕様

更新日：2026-09-15

今後追加するツールページは、本書と現在の実装を基準にする。従来のLetters参照仕様を置き換え、現行のコンパクトな業務ツールのデザインを採用する。

## 1. 基本方針

- キャッチコピー・大型ヒーロー・中間CTAを置かず、ページ名からツールリンクや操作へ直接進める。
- 淡いグレーの背景に白いカードを並べ、用途ごとに入力と結果をまとめる。
- カード・入力欄・ボタン・タブ・結果面の角丸は **5px** に統一する。ピル型や大きな角丸へ変更しない。
- 見出し・フォーム・カードは下記の実務向けサイズを維持する。紹介サイト用の大きな文字や余白へ拡大しない。
- 新規ページは内容に応じて「複数計算カード型」「入力＋集計型」「リファレンス型」を選ぶ。全ページを同一カラム構成にする必要はない。
- 計算条件・料金設定は各ツール固有の仕様とし、デザインの流用だけで他のツールへ引き継がない。

## 2. 正本となる実装

| ページ | ファイル | 用途 |
|---|---|---|
| ツール一覧 | `index.html` | 各ツールへ直接リンクするカード一覧 |
| 不動産計算 | `F_App/F_App.html` | カテゴリータブ＋複数の計算カード |
| 制作費見積もり | `Design_estimate/Design_estimate.html` | 媒体選択＋入力カード＋集計サイドバー |
| InDesign | `InDesign_settings/InDesign_settings.html` | 設定・使い方メモ |
| Illustrator | `Illustrator_settings/Illustrator_settings.html` | 設定・使い方メモ |
| Photoshop | `Photoshop_settings/Photoshop_settings.html` | 設定・使い方メモ |
| おすすめフォント | `Recommended_fonts/Recommended_fonts.html` | フォント名のリファレンス＋推薦フォント一覧 |
| 印刷物制作について | `Print_production/Print_production.html` | オーバープリントと黒の実務リファレンス |

過去の公開サイトではなく、上記ローカルファイルの現行状態を基準とする。フォント・配色・角丸などは公開当初から変更済み。

## 3. 配色

### 共通の色

| 用途 | 色 | 適用 |
|---|---|---|
| ページ背景 | `#f1f4f6` | `--bg` |
| カード背景 | `#ffffff` | `--surface` |
| 入力・サブ面 | `#f5f7f9` | `--surface2`（使用するページのみ） |
| 境界線 | `#d9dee3` | `--border` |
| 基本文字 | `#303030` | `--text` |
| シアン | `#00d7e6` | メインまたはサブカラー |
| ピンク | `#ff5281` | メインまたはサブカラー |
| 計算結果・見積金額の地色 | `#001410` | `--result-bg` |
| 注意書き・補足メッセージ | `#341f8a` | `--message-color` |
| キーボードフォーカス | `#185f83` | 3pxのアウトライン、外側3px |

`#001410` はメインカラーではなく結果面の背景に使う。坪⇄平米換算の「切替」ボタンの通常背景も、指定された例外としてこの色を使う。

### ページ別の使い分け

| 項目 | 一覧 | F App | 制作費見積もり |
|---|---|---|---|
| メイン | シアン `--accent` | シアン `--primary` | ピンク `--accent` |
| サブ | ピンク `--accent2` | ピンク `--accent` | シアン `--accent2` |
| 補助文字 | `#9f9f9f` | `#74777a` | `#9f9f9f` |
| 薄い補助文字 | — | `#b0b0b0` | `#b0b0b0` |
| カード見出し | `#303030` | シアン | ピンク |
| カードマーク | F Appはシアン、見積もり・Adobeメモ3枚・おすすめフォントはピンク | ピンク | シアン |
| 選択中タブ | — | ピンク背景・白文字 | ピンク背景・白文字 |
| 結果の強調数値 | — | ピンク | 白 |

F Appのヘッダー背景は他ページと同じ `#f1f4f6`（`--bg`）。入力フォーカスの境界は現状 `#337aff`、背景は白、影は `0 0 0 3px rgba(42,90,140,0.12)`。

見積もりの特急対応・一部補助表示には現状 `#fef5f1` の薄い背景と `#f8d5c8` の境界が残る。これは「※」注意書きの背景には使わない。

### 注意書きと変数管理

- **「※」の注意書きは背景色なし、文字色 `#341f8a`。** ラベル内の場合は注記部分だけに適用する。
- `.caution-note` を使い、既存クラスの背景指定より後で適用する。
- `--green` は廃止済み。色名ではなく用途を示す `--message-color` を使用する。
- `--green-bg` は廃止済み。注意書きに紫の背景面を復活させない。
- 使用しない色変数は追加せず、削除時はCSS・HTML・JavaScript内の参照を確認する。

```css
.caution-note {
  background: transparent;
  color: var(--message-color);
}
```

F App・おすすめフォントの現行実装では注意書き色を直接指定している。新規ページで共通化する場合は上記の用途名を使う。

## 4. 書体と文字サイズ

日本語・基本UIは **Noto Sans JP**、ロゴ・金額などの欧文数値は **Montserrat**。Google Fontsから必要なウェイトを読み込む。Open Runde・Zen Maru Gothicは現在のページでは使用していない。

| 要素 | 一覧 | F App | 制作費見積もり |
|---|---|---|---|
| ページ名・ロゴ | 一覧見出し24px/700、ロゴ20px/700 | ロゴ18px/700 | 見出し20px/700 |
| 区画見出し | — | 13px/700 | ラベル11px/700 |
| カード見出し | 17px/700 | 14px/700、行高1.3 | 13px/700 |
| カード説明 | 13px、行高1.6 | 11px | 11〜12px |
| フォームラベル | — | 12px/500 | 11px/700 |
| 入力文字 | — | 15px | 12〜13px |
| タブ | — | 12px | 12px |
| 主な結果数値 | — | 22px/700 | 28px/500 |
| 複数結果の数値 | — | 16px/700 | 内訳12〜13px |
| 注意書き | — | 11px、行高1.5 | 11px、行高1.6 |

F Appの本文は15px、行高1.7。見積もりの金額・数量などにはMontserratを用いる。新規ページで日本語を含むボタンにMontserratを使う場合も、日本語フォールバックを確保する。

## 5. レイアウト

以下は現行CSSの値。余白を増やした改修案ではなく、現在の構成を記録する。

### ツール一覧

- ヘッダー：左右60px・上下26px、下境界1px。
- メイン：最大1100px、中央配置、内側 `100px 60px 120px`。
- 見出し：「ツール一覧」、24px。キャッチコピーは置かない。
- 一覧：flexで折り返し、間隔20px、上マージン64px。
- リンクカード：幅270px、内側上下32px・左右28px。カード全体をリンクにする。
- カードマーク：34×34px、下余白20px。見出しと短い用途説明を配置。

### 複数計算カード型（F App）

- ヘッダーは上端にsticky。内側最大1200px、上下14px・左右20px。
- ヘッダー内にロゴとカテゴリータブ。タブは横並び、間隔4px、小画面では横スクロール。
- メイン：最大1200px、内側 `24px 16px 48px`。
- グリッド：`repeat(auto-fill, minmax(320px, 1fr))`、間隔16px。1280px画面では3列が目安。
- 600px以下は1列。
- カード：内側20px。見出し行の下余白16px。別グループとの間は32px。
- 入力フィールドの下余白12px、入力同士の横間隔8px。

### 入力＋集計型（制作費見積もり）

- ページ全体の内側：`24px 16px 80px`。
- ヘッダーとメイン：最大980px、中央配置。ヘッダー下余白24px。
- 見出し左・「トップへ」右のflex配置。`align-items:center`、`justify-content:space-between`、間隔12px、折り返しあり。ボタンはヘッダー内に置く。
- メイン：`1fr 310px` の2列、間隔20px、上揃え。
- 左：媒体タブ→制作内容→追加作業などのカード。
- 右：集計カード。上端20pxでsticky。
- 760px以下：1列になり、集計は入力群の後に続く。
- カード下余白14px、ヘッダー内側 `13px 18px 12px`、本文内側 `16px 18px`。
- カード内2列フィールド：間隔10px、480px以下は1列。
- タブは折り返し、間隔6px、内側 `10px 14px 14px`。

## 6. 部品の仕様

### 枠・影

- 通常のカード境界：1px。見積もりの入力・選択部品は1.5px。
- 角丸は一律5px。小さいマークは寸法によって円形に見える場合もある。
- F Appカードの影：`0 2px 12px rgba(48,48,48,0.06)`。
- 見積もりカードの影：`0 1px 12px rgba(48,48,48,0.06)`。
- 一覧カードの影：`0 2px 16px rgba(48,48,48,0.06)`。
- hover時：一覧は上へ4pxと影の強調、F Appは影のみ強調。見積もりの通常カードは浮き上がらせない。

### 入力・選択ボタン

- F App入力：高さ38px、左右12px、背景 `#f5f7f9`、文字 `#303030`。
- 見積もりの標準select：文字13px、内側 `9px 32px 9px 12px`、背景白。
- 見積もりのサイズ・面数ボタン：内側 `8px 14px`、文字12px/500、間隔8px、折り返し。
- 選択ボタンは通常白地、選択中はメインカラー背景・白文字。`aria-pressed`を選択状態と同期する。
- 関連する選択肢は同じボタンデザインを使う。名刺の「サイズ」と「デザイン制作」が実例。
- 入力の可視ラベルと読み上げ名を対応させる。

### 結果表示

- F App単独結果：背景 `#001410`、内側 `14px 16px`、上余白14px。
- F App複数結果：2列、間隔8px、各面内側 `10px 12px`。
- 結果ラベル・単位・補足は白系、主要数値はピンク。
- 見積もり合計面：背景 `#001410`、白文字、内側 `18px 20px 16px`。
- 見積もり内訳面：白背景、内側 `14px 18px`、区切りは薄い破線。税額区切りはメインカラー。
- ローン内訳は現状、白とサブ背景の行で構成する。全結果を無条件に暗い背景へ変更しない。

### 左右入力の切替

坪⇄平米換算はラベル行と操作行を分離する。

```css
.area-conversion {
  display: grid;
  grid-template-columns: minmax(0, 1fr) auto minmax(0, 1fr);
  gap: 6px 8px;
  align-items: center;
}
```

- 左右の入力は同じ背景色・文字色。読み取り専用であることによって別の地色を付けない。
- 入力と切替ボタンを同じ行に置き、縦中心を揃える。ボタンの固定上マージンで位置を合わせない。
- 切替ボタン：通常背景 `#001410`・シアン文字、hoverはピンク背景・白文字。

## 7. 動作と確認

- 計算は入力・選択変更時に更新。デザイン変更で計算式や料金を変えない。
- タブは矢印・Home・Endキーで移動し、選択状態とフォーカスを同期する。
- フォーカス表示は `outline: 3px solid #185f83; outline-offset: 3px`。
- hoverなどの短い変化は150〜200ms程度。`prefers-reduced-motion: reduce`で移動・アニメーション・transitionを停止する。
- 新規追加・変更後は360/390/768/1280/1440px、文字200%、全タブ、長い数値、代表的な計算例を確認する。
- 指定色と寸法は現行実装の記録であり、全配色・状態のアクセシビリティ適合を検証済みとするものではない。

## 8. 新しいツールを追加するとき

1. 用途に近い現行ページの構成を選ぶ。
2. `ToolName/ToolName.html` にHTML・CSS・JavaScriptを配置する。現在は各ページ内にCSSと計算処理を持つ構成。
3. 配色の主副を決め、実際に使用する変数だけ定義する。結果背景と注意書き色は共通仕様に従う。
4. ロゴ・見出し・入力項目・結果を新しいツール自身の内容へ置き換える。
5. `index.html` に直接リンクするカードを追加する。
6. 表示・キーボード操作・計算結果を確認し、デザイン方針の変更があれば本書を更新する。

現行HTMLに残る未使用クラスやマークアップ上の偶発的な不備はテンプレート仕様ではない。新規ページには使う部品だけを移し、ナビゲーションなどの表示要素は`body`内に置く。既存の `fonts/` 内のOpen Rundeファイルは新規ページへ自動で組み込まない。


## 9. Adobeメモ・トップへの導線

### 「トップへ」

- リンク先は `../index.html`。ページ先頭へのスクロールではなく、ツール一覧に戻るリンク。
- 見積もり・Adobeメモ3ページ・おすすめフォントはシアン `#00d7e6` 背景・白文字。F Appはピンク `#ff5281` 背景・白文字。
- `display:inline-block`、余白 `3px 9px`、文字 `10px / 700`、字間 `0.5px`、角丸 `5px`、下線なし。
- Adobeメモ・おすすめフォントは本文の行高1.8を引き継がず `line-height:normal` を指定。見積もりとAdobe3ページは過去の確認環境で60×21px。固定幅・固定高さではなく上記指定で揃える。F Appの行高は既存1.7のまま。
- ホバー時も下線なし、背景・文字色は維持。キーボードフォーカスは共通の3pxアウトライン。
- Adobeメモ・制作費見積もり・おすすめフォントでは見出し行の右に配置し、上下中央を揃える。F Appではヘッダー内に配置する。

### Adobeアプリ間リンク

- 通常：白背景、文字 `#303030`、枠 `1px solid #d9dee3`、余白 `7px 13px`、文字12px、角丸5px。
- ホバー：枠と文字がピンク `#ff5281`、下線なし、`transition:all 0.15s`。
- 現在ページ：`aria-current="page"`、ピンク背景・ピンク枠・白文字・太さ700。ホバーしてもこの選択表示を優先。
- 見積もりタブから揃えたのはホバーの色・下線なし・遷移時間。通常時の白背景・1px枠はAdobeメモ固有の現行仕様。
- ナビは間隔6pxで折り返す。通常の本文・目次リンクにはボタン用の下線抑制を広げない。
- `prefers-reduced-motion:reduce` ではtransitionを停止する。

### メモページの構成と配色

- ピンク主色・シアン副色。ページ見出しとカード見出しはピンク、カード先頭の10pxマークはシアン。
- 最大幅980px、本文＋右260pxの目次・参考資料、間隔20px。760px以下は1列で目次側が先頭。
- 本文13px・行高1.8、カード見出し13px、ページ見出し20px。既存のカード余白・5px角丸を維持。
- InDesign・Illustrator：最初に変えておきたい設定、よく使うショートカット一覧、参考資料。
- Photoshop：最初に変えておきたい設定、プロファイル警告と処理方法、入稿準備、レタッチTips、参考資料。
- 空のカテゴリーと「例：〜追記」の仮置き文は削除済み。具体的な操作の数値例は残す。詳細は `MEMO_EDITING.md`。
- 設定項目は入れ子の`ul`・`li`で表示。Illustratorの「環境設定」内には「ブラックのアピアランス」を配置し、太字の設定名＋設定値で表示する（スクリーン：すべてのブラックを正確に表示／プリント・書き出し：すべてのブラックを正確に出力）。既存設定と同じリストの余白・文字サイズを使う。

### indexの配色範囲

- index全体の主副色は従来どおりシアン主色・ピンク副色。見出しとロゴ文字は `#303030`。
- カードマークのみ、F Appはシアン、見積もり・InDesign・Illustrator・Photoshop・おすすめフォントはピンク。
- カードホバーの枠はF Appのみシアン `#00d7e6`、それ以外はピンク `#ff5281`。上へ4pxの移動・影・遷移時間は共通。

## 10. おすすめフォント

### ページ構成

- 正本：`Recommended_fonts/Recommended_fonts.html`。基本書体はNoto Sans JP。
- ページ余白 `24px 16px 80px`、最大幅980px。本文13px・行高1.8、長い名称は折り返す。
- 見出し20px/700・ピンク、行高1.5。見出し行の下余白16px。「トップへ」は右側・上下中央。
- 大分類は同じ階層の`h2`で、以下の順に置く。カテゴリ間は32px。

| 順番 | カテゴリ | ID | 現在の構成 |
|---|---|---|---|
| 1 | フォント名の見方 | `font-names` | リファレンス8区画 |
| 2 | 日本語フォント | `japanese` | 20カード |
| 3 | 欧文フォント | `western` | 26カード・アルファベット順 |
| 4 | 美しい数字の書体 | `numerals` | 15カード |

- ページ上部のカテゴリナビも同じ順序。各カテゴリへのページ内リンクで、切替タブではない。
- ナビは間隔6pxで折り返し、下余白20px。白背景・1px境界・5px角丸、余白7px 13px、文字12px。
- ナビのホバーは文字と境界がピンク、遷移150ms。選択中の固定表示は付けない。
- カテゴリ見出しは13px/700・ピンク。先頭に10pxのシアンマーク、間隔9px、下余白14px。リンク移動時の上余白20px。

### 推薦フォントのカード

- 白背景・1px境界・角丸5px、影 `0 1px 12px rgba(48,48,48,.06)`。
- 2列の`minmax(0,1fr)`、上揃え、間隔14px。760px以下は1列。
- カード余白16px 18px。カード全体をリンクにせず、ホバーで浮き上がらせない。

| 要素 | 文字・色 | 余白・行高 |
|---|---|---|
| フォント名 | 17px/700、`#303030` | 行高1.5、下8px |
| おすすめ：推薦者 | 13px/500、`#74777a` | 行高1.8 |
| メモ・特徴 | 13px/400、`#74777a` | 上10px、段落間10px、行高1.8 |

- 順序はフォント名→推薦者→メモ。メモがない場合は説明を追加しない。推薦者が未記載の場合はおすすめ欄を表示しない。
- 日本語名は名称そのものを表示し、メーカー等の補足を名前へ混在させない。欧文は提供された読みがある場合のみ「欧文名（カタカナ読み）」とする。
- 欧文の並び順は読み表記を除く英字名で比較し、大文字・小文字を区別しない。
- 同一カテゴリ内の同一書体は統合し、元の推薦者・メモを保持する。複数推薦者は「おすすめ：hamkoさん / ミヤマ」のように併記する。
- 名称が似ているだけの別書体・別版は勝手に統合しない。ユーザー指定で統合済みの欧文4組は次の名称を使う。

| 統合見出し |
|---|
| DIN（ディン） / DIN 2014 |
| Franklin Gothic（フランクリンゴシック） / Franklin Gothic ATF |
| Futura（フーツラ） / Futura PT |
| Gill Sans（ギルサン） / Gill Sans Nova |

- Futuraのメモには「Futuraの代替フォント」とJost・Gilroy・Centra No2・Codec Proのリストを掲載。
- 数字カテゴリは用途が異なるため欧文との重複を許容し、各用途のメモを保持する。数字側のDIN 2014・Futura PTは独立した名称のまま掲載する。
- 数字見本は実書体を正確に読み込める場合のみ検討。現在はWebフォントを用意していないため表示しない。別書体による代用表示はしない。

### フォント名の見方

- 推薦カードとは別の1列リファレンス。以下の順序を維持する。

1. ベンダーの省略記号
2. プロポーショナルフォント
3. ProやStdなどの意味（グリフセット）
4. Nフォント
5. 使う上でのポイント
6. 多言語フォント
7. フォントの種類
8. フォント使用時の注意

- 区画は白背景・1px境界・角丸5px・推薦カードと同じ影。余白16px 18px、区画間14px。
- 区画見出しは13px/700・ピンク。下に1px罫線、下パディング10px・下マージン12px。本文は13px・行高1.8。
- 略語は太字の定義リスト。略語1：説明2の2列、各列は`minmax(0,…)`で折り返す。行上下7px、下罫線1px、略語列の右余白12px。
- 通常の段落・リストは下10px、リスト左20px・項目間6px。小見出しは13px/700。
- 「使う上でのポイント」はピンクの左罫線4px、重要語もピンクの太字。
- 「※」の補足は背景なし・紫 `#341f8a`・12px。
- 「フォントの種類」の6種と「フォント使用時の注意」の2項目は、13px太字の小見出し、項目間の上罫線1px・上余白16px・上パディング16pxで区別する。
- OpenType、TrueType、Type 1、OpenType-SVG、Windows、macOS、Adobe CCなど判断用キーワードは太字。
- 注意区画は紫の左罫線4pxと紫見出し。Adobe Fontsの提供終了文は紫の太字で強調し、警告用の強い背景色は加えない。
- 提供文章の内容・意味は変更せず、資料にない説明を補完しない。

### スクロールに追従する「上に戻る」

- 表示は「↑ 上に戻る」、リンク先は`#page-top`。ツール一覧に戻る「トップへ」とは遷移先が異なる。
- 画面右下に常時固定、`z-index:10`。右・下は16pxとセーフエリアの大きい方。
- 「トップへ」と`home-btn`クラスを共用。シアン背景・白文字10px/700、余白3px 9px、行高normal、字間0.5px、角丸5px、下線なし。
- 幅・高さは固定せず文言と共通余白で決まる。独自の最小高さ・枠・影は付けない。
- キーボードフォーカスは共通仕様。印刷時は非表示。

## 11. 現行仕様の確認範囲

- 2026-09-15時点のローカルHTML・CSSと照合した仕様。公開サイトの反映状態を示すものではない。
- 今回の仕様整理ではサイトHTML・計算処理を変更しない。
- 直近の追加・修正はソース上の構成、既存内容保持、リンク、差分を確認済み。画面幅別・文字200%・実ブラウザの表示確認は未実施のため、第7節の確認項目を実施済みとして扱わない。

### Photoshopのプロファイル警告

- `profile-warnings`を最初の設定カードの次に配置し、目次にも同じ順でリンクする。判別説明→RGB→CMYKの順。
- RGB・CMYKは1px枠・5px角丸・余白16px 18pxの別区画。2列・間隔14px、1000px以下はRGB→CMYKの1列。見出し13px、操作名は太字、注意点は共通の紫・背景なし12px。

## 12. 印刷物制作について

- ピンク主色・シアン副色、背景#f1f4f6。既存Adobeメモと同じ最大980px、本文＋右260px目次、間隔20px。760px以下は目次先頭の1列。
- 4カードを比較・自動スミノセ・3種類の黒・実案件例の順に配置。本文13px/1.8、ページ見出し20px、カード・小見出し13px、余白16px 18px、角丸5px。
- オーバープリントと乗算は2列比較、600px以下は1列。黒の用途は2列表。カラー値は既存code相当の薄い背景・太字、注意は紫12px・背景なし。
- トップへは見出し行右・シアン背景の共通仕様。indexカードはピンクのマーク・ホバー枠。
