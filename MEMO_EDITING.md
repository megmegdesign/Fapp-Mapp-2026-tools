# メモページの追記方法

- 各ページは `App_settings/App_settings.html` の単体HTMLです。CSSと本文を同じファイルに保持します。
- 本文の項目は `section` 内の `ul` に `li` を追加します。小項目は入れ子の `ul` に追加します。
- カテゴリー追加時は `section` に一意なIDを付け、目次にも同じIDへのリンクを追加します。
- 参考PDFの共有URLを取得したら「参考資料」のコメント内にあるリンク例を有効にし、「共有リンク設定待ち」を置き換えます。`target="_blank"` と `rel="noopener noreferrer"` は維持してください。
- 参考PDF本体はリポジトリへ配置しません。
- 初期原稿はAI-memoryの `workspace/tool-site/inbox/20260822_*設定・使い方メモ案.md`。元Markdownは保持し、以後のユーザー指示によるHTML側の削除・追記を優先します。原稿から再転記する際に削除済みの仮置き例文やカテゴリーを復活させないでください。
- ボタン・ホバー・配色の正本は `design.md` 第9節です。

## 現在のカテゴリー（2026-09-11）

- InDesign・Illustrator：`section-1` 最初に変えておきたい設定、`section-2` よく使うショートカット一覧。
- Photoshop：`section-1` 最初に変えておきたい設定、`section-4` 入稿準備、`section-5` レタッチTips。
- 各ページの参考資料は `references`。残存IDは振り直さず、削除・改題時は目次も同期します。
- 「例：〜具体的な項目を追記」の仮置き文は掲載しません。実際の操作説明に含まれるカラー値などの数値例は保持します。
- InDesignの環境設定はCreative Cloud環境設定の後に配置。高度なテキスト・辞書・黒の表示方法をユーザー提供文で掲載済みです。連数字処理・全角スペースの行末吸収・禁則調整方式など段落パネル側の設定はこの項目へ混在させません。
- 指定された設定値・操作説明を黙って修正せず、疑問点は確認事項として報告します。直近の環境設定は提供文の転記で、操作内容の外部検証は未実施です。

## 今後の進め方・持ち越し

- 新規ページは先にチャットで指示書を作成し、内容確認後に実装します。
- 既存ページを引き継ぎ、重複ページやビルド環境を追加しません。
- 参考PDFの制限付きGoogle Drive共有URLは未設定。取得までは「共有リンク設定待ち」を維持します。
- InDesign書籍の未転記部分は必要に応じて別途指示を受けます。今回の環境設定3分類は追記済みです。
- 今回の作業はローカル反映で一区切り。コミット・公開は別途指示を受けてから行います。
